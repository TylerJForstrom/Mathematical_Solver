[project]
name = "mathematical-solver"
version = "0.1.0"
description = "A tree-based propositional logic solver for evaluating mathematical statements."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "TylerJForstrom" }]

[project.scripts]
math-solver = "logic_solver.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]

